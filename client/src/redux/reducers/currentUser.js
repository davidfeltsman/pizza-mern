const initialState = {
  user: {},
  isAuthorizate: false,
};

const currentUser = (state = initialState, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default currentUser;
